[automationscript]
requirement = "for each inbound record, check the conditions: if the assetnum starts with MFAC, change it to NPR. if it starts with APPR, skip the record."
# type = 1  # 0 = general, 1 = inbound integration, 2 = outbound integration


[integration]



# needed for an integration

# 3. the object structure. 
# 2. the json mapper.
# 1. the automation script.
    ## name
        ## inbound  
        # .or.
        ## outbound
    ## description
# 4. the enterprise service.
# 5. the external service. 

# 6. the endpoint